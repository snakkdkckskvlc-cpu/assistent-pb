# Редизайн интерфейса — август 2026

Спецификация. Описывает, каким интерфейс должен стать и почему именно таким.
Текущее состояние — в [design-system.md](design-system.md); он остаётся в силе,
пока редизайн не внедрён, и заменяется по факту внедрения.

Источник фирменного стиля — «Маркетинг-кит 7.0 от 07.08». Цвета сняты с файла
пипеткой, шрифт вытащен из встроенных в PDF, логотип извлечён с прозрачностью.
Ничего не подобрано на глаз.

---

## 1. Что не так сейчас

Обратную связь от сотрудников не собирали, поэтому находки ниже получены
разбором кода и проходом по сценариям, а не опросом. Каждая помечена:
**замерено** — видно в коде и воспроизводится, **гипотеза** — требует проверки
на живом человеке.

### 1.1. Ломается работа

| № | Находка | Где | Статус |
|---|---|---|---|
| 1 | `submitForm` блокирует кнопку по жёсткому `id="submit"`. На проверке документа кнопок две, и «Глубокая проверка» во время работы остаётся активной — второй клик ставит вторую задачу в очередь на тех же файлах | [`app.js:221`](../../apps/desktop/frontend/app.js), [`spellcheck.html:34`](../../apps/desktop/frontend/views/spellcheck.html) | замерено |
| 2 | Запущенную задачу нельзя отменить. Разбор договора идёт 3–8 минут; ошибочный запуск занимает очередь на сервере, где работают тридцать человек | `app.js::pollTask` | замерено |
| 3 | Уход со страницы теряет результат. Опрос задачи живёт в DOM страницы, история хранит только сводку и не даёт открыть или скачать прошлый результат | `app.js:241`, [`history.html:86`](../../apps/desktop/frontend/views/history.html) | замерено |
| 4 | В плашку ошибки уходит сырое тело ответа сервера — то есть текст, написанный для программиста | `app.js:234, 252` | замерено |

### 1.2. Мешает понимать

| № | Находка | Где | Статус |
|---|---|---|---|
| 5 | В анализе договора вердикт и сводка плюсов-минусов идут **после** всех находок. Руководителю, которому нужно «за минуту оценить картину» (US‑2.1), приходится пролистать двадцать карточек до ответа | [`legal.html:82–136`](../../apps/desktop/frontend/views/legal.html) | замерено |
| 6 | Разбор договора никуда не выгружается. У проверки документа есть DOCX, у пакетной — сводный отчёт, у одиночного анализа — ничего, только копирование руками | `legal.html` | замерено |
| 7 | Правки орфографии нельзя принять выборочно: исправленный DOCX приходит со всеми сразу | `spellcheck.html:93–105` | замерено |
| 8 | В шапке висит `qwen2.5:14b-instruct-q4_K_M готова · нормативная база подключена · LanguageTool подключен`. Для ИТ‑админа (US‑4.3) это полезно, для секретаря — шум из непонятных слов | `app.js:76` | замерено |
| 9 | В подписи прогресса показывается идентификатор задачи (`id: 8f3a…`) | `app.js:238` | замерено |
| 10 | Страница «История» смешивает две несвязанные вещи — журнал задач и состояние защиты данных, причём во второй сотруднику предлагают выполнить `manage-bde -status` и `git reset --hard origin/main` | `history.html:129, 154` | замерено |
| 11 | Кнопки 👍/👎 требуют трёх действий: выбрать оценку, ввести комментарий, нажать «Отправить». Похоже, поэтому обратной связи и нет | `app.js:158–161` | гипотеза |

### 1.3. Мешает работать быстро

| № | Находка | Где | Статус |
|---|---|---|---|
| 12 | Переключение между функциями стоит два шага: «назад на главную» → выбрать плитку. При четырёх функциях в ежедневном обороте это налог, который платят десятки раз в день | `index.html` | замерено |
| 13 | Страница «Транспорт» — это пять разных работ в одной простыне: выдача машины, машины в рейсе, парк с формой на 18 полей, справочник точек, журнал рейсов | [`transport.html`](../../apps/desktop/frontend/views/transport.html) | замерено |
| 14 | Страница «Путевые листы» — то же самое: редактор листа плюс три справочника (водители, прицепы, организации) | [`waybill.html`](../../apps/desktop/frontend/views/waybill.html) | замерено |
| 15 | В путевом листе поля «выезд» и «возврат» стоят рядом, хотя между ними рабочий день; обязательное для формы неотличимо от необязательного; незакрытая форма теряется целиком | `waybill.html` | замерено |

### 1.4. Выглядит дёшево

| № | Находка | Статус |
|---|---|---|
| 16 | Иконки — эмодзи 📝⚖️✉️🚚📦. Рисуются шрифтом системы, на Windows выглядят иначе, чем на Mac разработчика, и в деловом документообороте читаются как несерьёзность | замерено |
| 17 | Акцент `#c8352b` — не фирменный цвет. Фирменный `#E31E24` чище и ярче | замерено |
| 18 | Плоская иерархия: заголовок 34 px, дальше почти всё 14–15 px серым. Глазу не за что зацепиться | замерено |
| 19 | Нет ни одного стиля фокуса, кроме поля бланка письма (`.letterhead-field:focus`). Работа с клавиатуры вслепую | замерено |
| 20 | Жёлтый `#d4a017` на белом даёт контраст **2.38 : 1** при норме 4.5 — «существенный риск» в договоре читается хуже остальных | замерено |

---

## 2. Навигация

Принята модель «рабочее место + сводка дня»: постоянная боковая панель и
стартовый экран «Сегодня». Основание — все четыре группы функций в ежедневном
обороте, то есть человек переключается между ними много раз за день (находка 12),
а задачи идут минутами, и результат сейчас теряется при уходе со страницы
(находка 3).

```
Сегодня                     ← стартовый экран, отвечает «что у меня происходит»
Документы
  Проверка текста
  Анализ договора
  Вопрос по файлу
  Проверить пачкой
Переписка
  Письма
Транспорт
  Машины и рейсы            ← выдача, машины в рейсе, журнал
  Путевые листы             ← только листы
Справочники                 ← вынесены из рабочих экранов (находки 13, 14)
  Парк и точки
  Водители и прицепы
Внизу: История · Иванов И. И.
```

**Экран «Сегодня»** содержит: плашку метрик (считается сейчас / машин в рейсе /
готово вчера / путевых за месяц), список «В работе» с процентом и остатком
времени, список готового со скачиванием. Он же решает находку 3: запустил
глубокую проверку, ушёл писать письмо, вернулся и забрал.

Ширина панели 186 px. На ноутбуке 1366 остаётся 1180 — формам и таблицам хватает.

---

## 3. Фирменный стиль

### 3.1. Цвет

| Токен | Значение | Где взят | Назначение |
|---|---|---|---|
| `--red` | `#E31E24` | логотип | действие и проблема — больше нигде |
| `--red-dark` | `#C1161B` | затемнение | наведение на кнопку |
| `--red-soft` | `#FDECEC` | — | подложка критичной находки |
| `--graphite` | `#27323D` | плашка «20+ / 100+ / 600 млн», заголовки кита | боковая панель, шапка, основной текст |
| `--graphite-2` | `#3B454F` | фон кита | вторичный текст на тёмном |
| `--bg` | `#F5F7F9` | поле кита | фон приложения |
| `--line` | `#E3E7EC` | — | рамки и разделители |
| `--muted` | `#6B7681` | — | подписи |
| `--ok` | `#1F7A45` | — | готово, подтверждённая норма |
| `--warn` | `#8A6400` | — | существенный риск (замена `#d4a017`, находка 20) |

**Правило красного, и оно жёсткое.** Красный означает «здесь действие или здесь
проблема»: главная кнопка, выбранный пункт меню, критичный риск. Как только им
красят заголовки и иконки для красоты, он перестаёт работать сигналом, и человек
перестаёт замечать критичный пункт договора.

Проверенные контрасты: графит на белом 13.0, пункт меню на графите 8.1, красный
на белом 4.69, белый на красном 4.69, `--muted` на белом 4.63 — все выше нормы
4.5. Старый жёлтый 2.38 из системы убран.

### 3.2. Шрифт

**Manrope**, лицензия OFL, вариативный — одним файлом закрывает начертания
200–800. Два подмножества (кириллица 14 КБ + латиница 24 КБ) кладутся в
`apps/desktop/frontend/fonts/`. Приложение остаётся офлайн: файлы локальные, в
интернет за ними никто не ходит.

Подключение — обязательно с `unicode-range`, иначе браузер тянет оба файла
всегда:

```css
@font-face {
  font-family: "Manrope"; font-weight: 200 800; font-display: swap;
  src: url("/static/fonts/manrope-cyrillic.woff2") format("woff2");
  unicode-range: U+0301, U+0400-045F, U+0490-0491, U+04B0-04B1, U+2116;
}
```

`U+2116` — знак «№», он нужен путевому листу и письму.

Шкала: заголовок 26/800, экран 19/800, раздел 15/700, текст 13/500, подпись
11.5/500, метка 10/700 прописными с трекингом. Цифры — всегда
`font-variant-numeric: tabular-nums`, иначе колонки пробега и топлива пляшут.

### 3.3. Иконки

Эмодзи убираются полностью (находка 16). Вместо них — штриховые SVG прямо в
разметке: 20×20, `stroke-width: 1.7`, `fill: none`, цвет наследуется от
родителя. Внешних иконочных шрифтов не подключается — правило офлайна.

Логотип — извлечён из кита, 185×185 с прозрачностью, в интерфейсе 24–26 px.

---

## 4. Экраны

### 4.1. Ожидание — сквозное состояние

Видят все и каждый день, поэтому описано первым. Три вещи, которых нет:

1. **Понятная стадия** вместо «Обработка»: «Разбираю пункт 9 из 26», «Осталось
   примерно 4 мин». Идентификатор задачи убран (находка 9).
2. **Разрешение уйти**: «Окно можно закрыть — задача считается на сервере,
   результат будет ждать на экране „Сегодня"». Плюс отдельная строка про прогрев
   модели на первом запуске — это уже описано в текущей дизайн-системе, но
   человеку показывается только косвенно.
3. **Кнопка «Отменить задачу»** (находка 2). Требует ручки на backend.

### 4.2. Проверка текста

Правка становится решением, а не отчётом: у каждой строки «Пропустить» и
«Принять», рядом «Принять все 23» (находка 7). Источник правки (LanguageTool /
правило / модель) остаётся, но уходит в конец строки — он нужен для доверия, а не
для чтения. Счётчики по типам ошибок выносятся над таблицей.

### 4.3. Анализ договора

Главная правка структуры: **вердикт первым** — «Подписывать нельзя без правок»,
одной фразой суть, рядом счётчик 3 / 5 / 4 по цветам (находка 5). Находки под
ним, отсортированы по критичности. Причина автоповышения уровня и статус
проверки статьи («найдена в переданных нормах») остаются — именно они отличают
разбор от догадки. Добавляется выгрузка разбора в DOCX (находка 6).

### 4.4. Путевой лист

Сотня полей разбивается на четыре шага в порядке реальных событий: машина и
водитель → выезд → задание и рейсы → возврат и итоги смены (находка 15).
Внутри шага исходные разделы бланка сохранены — это язык, на котором про лист
говорят. Блоки прицепов, ездок и простоев переезжают в свои шаги.

Поля рисуются все, прячутся лишь чужие шаги: сборка (`collectWaybill`) читает
бланк одним проходом, и графы других шагов иначе молча не доехали бы до
сервера, а пропавшая графа путевого листа — недействительный документ.

**Отступление от первоначального замысла.** Счётчик у шага показывает факт —
«6 из 13 заполнено», а не «6 из 34 обязательных». Какие графы обязательны,
знает приказ, а не интерфейс, и по этой цифре сдают документ: выдумывать её
нельзя. По той же причине отложены пометки автоподстановки («подставлен из
прошлого листа») — backend сейчас не сообщает, какие значения он подставил сам.

Справочники (водители, прицепы, организации) уезжают в раздел «Справочники».

### 4.5. Машины и рейсы

Остаётся только ежедневное: выдать машину, кто в рейсе, журнал. Парк и постоянные
точки уходят в справочники (находка 13).

### 4.6. История

Разделяется на две страницы (находка 10): журнал задач с возможностью открыть и
скачать прошлый результат, и «Защита данных» — отдельно, с текстами без команд
консоли. Формулировки для сотрудника: не «выполните `manage-bde -status`», а
«проверить это может только администратор — покажите ему этот экран».

### 4.7. Письмо, вопрос по документу, пакетная проверка, вход

Структурно не меняются, пересобираются из тех же блоков: шапка экрана, панель,
поля, кнопки, состояния. В письме реквизиты бланка перестают дублироваться
строкой в JS и приходят с backend — сейчас `LETTERHEAD_REQUISITES` в
`letter.html` живёт отдельно от `letterhead.docx` и разъедется при первой правке
реквизитов.

---

## 5. Чего не трогаем

- **Заголовки страниц и favicon** — люди находят вкладку по ним (правило проекта).
- **Статус готовности в шапке** — он нужен ИТ‑админу (US‑4.3). Но текст делится
  на две части: сотруднику «Готов к работе», подробности с именем модели — по
  наведению и на странице защиты данных (находка 8).
- **Вход по логину без пароля** и ссылка «не я».
- **Сборка**: фронтенд остаётся ванильным, без npm и препроцессоров. Токены —
  обычные CSS-переменные, иконки — инлайновый SVG.
- **Офлайн**: ни одного внешнего запроса, шрифт лежит локально.

---

## 6. Порядок внедрения — сделано

Все семь шагов внедрены в ветке `design/firmennyy-redizayn`.

Правки затрагивают файлы из `integrity.json`, поэтому в том же коммите
пересобирается манифест (`scripts/build_integrity_manifest.py`) — иначе
приложение откажется стартовать.

| Шаг | Состояние |
|---|---|
| 1. Токены, шрифт, иконки, каркас с боковой панелью | сделано |
| 2. Экран «Сегодня» + `GET /api/dashboard` | сделано |
| 3. Ожидание и отмена задачи (`POST /api/tasks/{id}/cancel`) | сделано |
| 4. Анализ договора: вердикт наверх, выгрузка в DOCX | сделано |
| 5. Проверка текста: выборочные правки (`POST /api/spellcheck/render`) | сделано |
| 6. Транспорт и путевой лист: шаги и вынос справочников | сделано |
| 7. История и защита данных врозь | сделано |

Сверх плана добавлено по просьбе пользователя: на «Сегодня» видно, занят ИИ
или свободен. Состояние общее на компанию — считающая задача одна, и человек
ждал чужую, не понимая почему.

**Манифест собирается только на чистом дереве.** Незакоммиченные правки
соседней сессии попадают в хеши молча, CI падает на «манифест ↔ дерево», а у
пользователя приложение отказывается стартовать с текстом, который выглядит
как «сломан ваш код». Проверенный способ — временный `git worktree` на HEAD
ветки; `git stash` для этого не годится, его `pop` конфликтует на самом
манифесте и затягивает чужие файлы в индекс.

---

## 7. Что осталось

Все двадцать находок аудита закрыты. Последние четыре — отдельным коммитом:

- **Прошлый результат открывается** — ссылки из истории и с «Сегодня» ведут на
  страницу функции с `?task=<id>`, разбор рисует родной для неё код.
- **Реквизиты бланка** читаются из самого `letterhead.docx` (`GET
  /api/letter/letterhead`), копия строкой в `letter.html` убрана.
- **Системные `prompt()`** заменены формами: выписка листа и возврат машины.
- **Автоподстановка помечена** — столбец `waybill.autofilled` хранит имена
  граф, заполненных программой из карточки машины.

Открытых пунктов не осталось. Строки открытия прошлой задачи в
`views/legal.html` уехали в репозиторий вместе с работой над редлайном
договора (23f0e6b) — ссылка на разбор договора из истории работает так же, как
у остальных функций.

### Найдено позже, на новых экранах

Аудит закрыт, но новые страницы приезжают и после него, и правило приходится
применять заново.

**Вкладки и кнопка действия одним цветом.** На экране «Где документ» вкладки
размечены кнопками, и выбранная получалась красной — точно такой же, как
«Завести документ» в том же ряду. Два разных смысла одним цветом: состояние
«я сейчас смотрю это» неотличимо от «нажми, и что-то произойдёт».

Добавлен компонент вкладок в `styles.css`: выбранная — графитовая, остальные
без фона. Разметка страницы не тронута, правила опираются на тот же класс,
которым её переключает свой же скрипт.

Уточнение к токену `--red` («действие и проблема — больше нигде»): в боковой
панели текущий пункт всё-таки красный, и это не исключение, а следствие. Панель
графитовая, графит на графите не читается. Формулировать правило надо не как
«красный нигде», а как **«в одном ряду не должно быть двух смыслов одного
цвета»** — так оно объясняет оба случая.

**Дата в списке путевых листов** показывалась как `2026-08-07`. Это машинный
вид: человек, который заполняет лист, пишет `07.08.2026`. Добавлен общий
`fmtDate` в `app.js`. Перестановка частей строки, без `new Date` — строка без
часового пояса разбирается как UTC, и западнее Гринвича дата уезжает на день
назад. Липецку (UTC+3) это ничем не грозит, но ошибка здесь — не тот срок в
документе, а к таким местам «у нас-то работает» не применяется.

### Замер ширины, а не ощущение

Целевые разрешения из требований — 1920×1080 и 1366×768, плюс окно приложения
неизвестного размера. Проверено программно: все пятнадцать страниц открывались
в рамке заданной ширины, и сравнивалось `scrollWidth` с `clientWidth`.

| Ширина | Результат |
|---|---|
| 1366 | переполнения нет ни на одной из 15 страниц |
| 1100 | переполнения нет (8 самых плотных страниц) |
| 900 | переполнения нет (те же 8) |

Первый прогон мерил страницы с тем, что лежит в базе, — а это единицы записей,
то есть проверял почти пустую вёрстку. Поэтому второй прогон подставлял данные
прямо в разметку: к каждой таблице дописывалось по 40 строк, а в ячейки —
название организации на 85 знаков.

| Страница | С 40 лишними строками и длинным названием |
|---|---|
| Путевые листы | переполнения нет |
| Машины и рейсы | переполнения нет |
| Водители и прицепы | переполнения нет (120 строк) |
| Парк и точки | переполнения нет (80 строк) |
| История | переполнения нет |
| Где документ | **не проверено** — данных для клонирования нет |

Глазами: длинное название переносится на вторую строку, соседние столбцы
сжимаются, но остаются читаемыми. В базу при этом ничего не писалось —
подстановка шла в разметку уже открытой страницы.

**Чего замер по-прежнему не показывает.** «Где документ» проверить было нечем:
журнал пуст, клонировать нечего. Вернуться к этому, когда в нём появятся
настоящие записи.

## 8. Что проверить на людях

Гипотезы, которые дизайн не подтверждает сам по себе:

- Четыре шага путевого листа — действительно ли они совпадают с тем, как
  заполняет механик. Проверяется одним заполнением рядом с ним.
- Кнопка фидбека в один клик — начнут ли нажимать (находка 11).
- Экран «Сегодня» — открывают ли его или сразу уходят в свою функцию. Видно по
  истории переходов.
- Строка «ИИ занят» — снимает ли она вопрос «программа зависла».
